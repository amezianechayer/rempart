---
description: Lancer la vérification complète et résumer les échecs par cause racine
---

Lance `make verify` puis `make evals`. Résume : ce qui passe, ce qui échoue regroupé par cause racine probable, régressions d'eval par rapport à la baseline. Ne corrige rien sans que je le demande.
